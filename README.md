# portofolio-infra

A small infrastructure repository for local development with Docker Compose, nginx, and Prometheus monitoring. It includes configuration for a reverse proxy and a Prometheus scrape target to help observe service health and traffic. This repo is intended as a lightweight environment for testing networking and monitoring setups locally. Use it as a starting point for deploying simple container-based applications and metrics collection.
